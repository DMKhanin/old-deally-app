import React from 'react';

const RedTemplateCover = ({ title, description }) => {
  return (
    <header>
      <h1>{ title }</h1>
      <p>{ description }</p>
    </header>
  )
};

export default RedTemplateCover;