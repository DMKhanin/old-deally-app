import { createContext } from 'react';

export const ProfilePageContext = createContext({});
ProfilePageContext.displayName = 'ProfilePageContext';

export const ShopListPageContext = createContext({});
ShopListPageContext.displayName = 'ShopListPageContext';

